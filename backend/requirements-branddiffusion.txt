# Full BrandDiffusion dependencies
# Install with: pip install -r requirements-branddiffusion.txt
# Note: This includes heavy ML dependencies. Ensure you have:
# - CUDA-compatible GPU (recommended)
# - Sufficient VRAM (8GB+ recommended)
# - Python 3.8-3.11

# Core dependencies (already in requirements.txt)
-r requirements.txt

# PyTorch (install appropriate version for your CUDA version)
# For CUDA 11.8:
# torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118
# torchvision>=0.15.0 --index-url https://download.pytorch.org/whl/cu118

# For CUDA 12.1:
# torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu121
# torchvision>=0.15.0 --index-url https://download.pytorch.org/whl/cu121

# For CPU-only (not recommended, very slow):
# torch>=2.0.0
# torchvision>=0.15.0

# Stable Diffusion and diffusion models
diffusers>=0.21.0
transformers>=4.35.0
accelerate>=0.24.0

# Computer Vision and Image Processing
mediapipe>=0.10.0
gfpgan>=1.3.8

# LLM and AI Services
groq>=0.4.0

# Model Optimization
peft>=0.6.0
bitsandbytes>=0.41.0  # Optional, requires CUDA

# Additional utilities
safetensors>=0.4.0
huggingface-hub>=0.19.0
